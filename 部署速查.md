# 班级宠物系统 · 部署速查表（Windows 服务器 / 不用 Docker）

> 把本文件一起复制到服务器上，忘的时候直接看这个。

## 0. 一句话记住

| 情况 | 要执行的命令 |
|---|---|
| **新服务器，第一次部署** | `node scripts/setup.js`（建表 + 默认账号），然后 `npm start` |
| **老服务器，更新代码** | 上传新文件 → 若含新的 `backend/migrations/` 文件，跑 `npm run migrate` → 重启后端 |
| **只要重启服务** | 重启后端即可，不要动数据库 |

> ⚠️ **`npm start` 只启动服务，不会建表！** 新服务器上直接 `npm start` 会出现"服务能开、但所有接口报 no such table"。

---

## 1. 目录约定（示例：D:\pet-laicun）

```
D:\pet-laicun\backend                    后端代码
D:\pet-laicun\backend\.env               后端配置（端口、密钥等）
D:\pet-laicun\backend\data\database.sqlite   ★ 数据库（所有数据都在这一个文件里）
D:\pet-laicun\backend\migrations\        数据库结构变更文件（001_、002_、003_…）
D:\pet-laicun\frontend\dist\             前端编译产物（交给 Nginx 的那个目录）
```

**多套系统（pet、pet2…）= 多份目录**：每份都有自己的 `.env`、端口、数据库，**升级时每份都要做一遍**。

---

## 2. 首次安装（新服务器）

### 2.1 后端

```cmd
cd /d D:\pet-laicun\backend
npm install
```

新建 `D:\pet-laicun\backend\.env`，内容：

```env
PORT=3001
NODE_ENV=production
JWT_SECRET=改成一串你自己的随机字符
JWT_EXPIRES_IN=7d
FRONTEND_URL=https://你的域名
AI_API_KEY=你的火山方舟Key
AI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
AI_MODEL=doubao-seed-2-1-pro-260915
```

初始化数据库（**只需第一次**，会建表并生成默认账号）：

```cmd
node scripts/setup.js
```

启动：

```cmd
npm start
```

> 推荐用宝塔面板的「Node 项目」托管：项目目录选 `backend`，启动选项选 `start`，它能自动守护进程、掉线重启。

### 2.2 默认账号（首次初始化只创建管理员）

```
管理员：admin / 111111        ← 上线前务必修改密码
```

**需要演示数据？** 登录管理后台 →「系统数据」→ 点「导入演示数据」。它会创建演示学校/班级 +
`demo_teacher1`、`demo_student1` 等**独立演示账号**（密码 111111），可随时一键清除，不影响真实账号。

同一个页面还能：
- 查看数据库结构状态、数据量统计
- 「立即执行迁移」（正常不需要，启动时已自动执行）
- 「重置为全新系统」→ 清空所有数据、恢复成刚初始化的状态（**不可恢复**，需输入确认词）

### 2.3 前端

在自己的开发机上：

```cmd
cd frontend
npm install
npm run build
```

把 `frontend\dist\` 整个目录上传到服务器（Nginx 站点 root 指向它）。前端**不需要**配置后端地址（默认走同源 `/api`）。

### 2.4 Nginx 要点（宝塔）

站点 root 指向 `frontend\dist`，另需两条：

```nginx
# 1) 单页应用回退，否则地址栏直接输网址会 404
location / { try_files $uri $uri/ /index.html; }

# 2) 反代后端（写在 站点设置→反向代理 里即可）
location /api/       { proxy_pass http://127.0.0.1:3001; }   # 端口 = 该实例 .env 的 PORT
location /socket.io/ { proxy_pass http://127.0.0.1:3001; }   # 聊天室，必须单独加
```

---

## 3. 日常更新（代码升级）

### 3.1 先判断：这次要不要动数据库？

**通常不用管**——后端启动时会自动检查并执行迁移，所以：

- 上传的文件里**有** `backend\migrations\` 下的新文件 → 重启后会自动升级表结构（看 3.3 的日志确认）
- **没有**（只改了 `src\` 里的代码） → 直接重启即可

> 唯一需要留意的：`migrations` 里的新文件也必须一起上传，否则后端"以为"没变化，不会升级结构。

### 3.2 更新步骤

```cmd
:: 1) 备份数据库（重要！改成当天日期）
cd /d D:\pet-laicun\backend
copy data\database.sqlite data\database.sqlite.bak-20260919

:: 2) 覆盖上传新代码（migrations 目录里的新文件也要传！）

:: 3) 安装依赖（只有 package.json 变化时才需要）
npm install
```

### 3.3 数据库升级（通常无需手动操作）

后端**启动时会自动执行数据库迁移**：上传新文件 → 重启后端，表结构会自动升级（新增字段、老数据回填）。

启动日志里会打印：

```
✅ 数据库迁移完成（批次 3，执行 1 个）: 003_xxx.js     ← 有结构变更
✅ 数据库结构已是最新，无需迁移                        ← 无结构变更
```

如需手动执行（或启动时迁移失败要重试）：

```cmd
cd /d D:\pet-laicun\backend
npm run migrate
```

也可以在管理后台「系统数据」页签查看结构状态，并点「立即执行迁移」。

### 3.4 前端更新

开发机 `npm run build` → 上传新的 `dist` 覆盖服务器上的 → 浏览器按 **Ctrl+F5** 硬刷新。

### 3.5 重启后端

宝塔面板里重启该项目（或结束 node 进程重新 `npm start`）。

---

## 4. ⚠️ 千万不要做的事

| 不要做 | 原因 |
|---|---|
| 跑 `node scripts/setup.js --clean` | 直接删除数据库文件，数据全没了 |
| 跑 `npm run db:reset` | 回滚全部迁移并重建，数据全没了 |
| 在有真实数据的系统上点「重置为全新系统」 | 清空所有数据、只留 admin（不可恢复，务必先备份） |
| 只传 `src` 不传 `migrations` | 后端以为"没变化"，表结构升不上去，会报缺列错误 |

> 说明：`node scripts/setup.js` 现在是**幂等且安全**的（只创建缺失的 admin 账号和基础配置数据，不会删除任何用户数据），
> 但**新服务器首次部署跑一次即可**，老服务器升级不需要它。

---

## 5. 出问题对照表

| 报错 / 现象 | 原因 | 解决 |
|---|---|---|
| `no such table: users` / `no such table: xxx` | 数据库没初始化（裸跑了 npm start） | 空库：`node scripts/setup.js`，然后重启后端 |
| `table xxx has no column named yyy` | 老库缺新字段（代码新、库旧） | 重启后端（会自动迁移）；仍失败则 `npm run migrate` |
| 注册/接口报 500 | 结构未升级，或看后端日志 | 先重启后端，再查日志；管理后台「系统数据」可看迁移状态 |
| 管理后台「系统数据」显示"迁移失败" | 迁移文件有问题或未完整上传 | 看页面上的错误信息 + 后端日志，补全 `migrations` 文件后点「立即执行迁移」 |
| 页面能开但接口 404/502 | Nginx 反代端口写错 / 后端没启动 | 确认 `proxy_pass` 端口 = `.env` 的 `PORT`，后端在跑 |
| 聊天室连不上 | Nginx 少配 `/socket.io/` 反代 | 补上第 2.4 节的 socket.io 配置 |
| 地址栏直接输网址 404 | 缺 SPA 回退 | 补上 `try_files ... /index.html` |
| 登录后过一会就要重新登录 | 多实例共用同一个 `JWT_SECRET` | 每套实例用不同的 `JWT_SECRET` |

**查日志**：宝塔该项目里看日志，或后端目录下的日志文件。

---

## 6. 备份 / 恢复（数据安全）

```cmd
:: 备份（建议每周 + 每次升级前）
copy D:\pet-laicun\backend\data\database.sqlite D:\backup\database-20260919.sqlite

:: 恢复：用备份文件覆盖回去，然后重启后端
copy D:\backup\database-20260919.sqlite D:\pet-laicun\backend\data\database.sqlite
```

> 所有数据（用户、班级、宠物、作业…）都在 `data\database.sqlite` 这一个文件里，**备份它就是备份了全部数据**。

---

## 7. 命令速查

```cmd
cd /d D:\pet-laicun\backend       :: 进入后端目录
npm install                        :: 安装依赖
node scripts/setup.js              :: 【仅新库】建表 + 默认数据
npm run migrate                    :: 【老库升级】只更新表结构
npm start                          :: 启动服务（不建表！）
npm run dev                        :: 本地开发用（改代码自动重启）

cd /d D:\pet-laicun\frontend
npm run build                      :: 编译前端，产物在 dist\
```
