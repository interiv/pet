# 班级宠物系统 — 手动部署指南（不使用 Docker）

本文档说明如何**不依赖 Docker / docker-compose**，直接在本机或服务器上部署「班级宠物养成系统」的前后端。

> 对应原 Docker 方案：镜像由 `Dockerfile` 构建，启动逻辑在 `backend/docker-entrypoint.sh`，编排在 `docker-compose.yml`。手动部署就是把容器里自动做的事，在宿主机上手动执行一遍。

---

## 一、架构说明

- **后端**：Node.js + Express + Socket.io，使用内置 **SQLite**（`better-sqlite3`），无需额外数据库服务。
- **前端**：Vite + React + TypeScript，编译产物为静态文件（`frontend/dist/`）。
- **两种托管方式**：
  - **方案 A（后端托管前端）**：把 `dist/` 复制到 `backend/public/`，由后端 Express 直接托管，访问后端端口即可同时得到前后端。
  - **方案 B（Nginx 托管前端）**：前端由 Nginx 直接托管（root 指向 `dist/`），后端单独跑在 3000，Nginx 反向代理 `/api` 和 `/socket.io` 到后端。
- 本文重点给出两种方案，并在最后一节给出**宝塔面板 + Nginx（方案 B）**的外网部署示例。

---

## 二、环境准备

- **Node.js**：项目基于 Node 25（Dockerfile 用 `node:25.9`），建议本地/服务器安装 **Node ≥ 20 LTS** 或 25.x。
- **npm**：随 Node 自带。
- **编译环境**（仅后端需要）：`better-sqlite3` 含原生模块，服务器需有 `python3` + `make` + `g++`（或已预编译的二进制）。
- **Git**（可选，用于拉代码）。
- 操作系统：Windows / Linux 均可，下文命令以 PowerShell / bash 区分注明。

---

## 三、数据库初始化体系（migrations / seeds / scripts）

| 目录 / 文件 | 作用 |
|---|---|
| `backend/migrations/` | 建表 / 改表结构（Knex 迁移）。`001_initial_schema.js` 定义全部数据表。`knex migrate:latest` 执行。 |
| `backend/seeds/` | 种子数据（初始 / 演示数据）。按序号拆分：`01_pet_species`(宠物物种)、`02_items`(道具)、`03_equipment`(装备)、`04_skills`(技能)、`05_achievements`(成就)、`06_tasks`(任务)、`07_forums`(论坛)、`08_users_and_classes`(用户与班级)、`09_test_data`(测试数据)。`knex seed:run` 填充。 |
| `backend/scripts/setup.js` | 一键初始化脚本，封装「迁移 + 种子」两步（支持 `--clean` 清库、`--db`、`--test-data` 等子命令）。`docker-entrypoint.sh` 内部正是调用它底层的 `knex migrate:latest` 与 `knex seed:run`。 |

> 简言之：`migrations` 建骨架，`seeds` 填内容，`scripts` 把两者串成一条命令。容器启动时自动执行，手动部署则手动执行一次。

---

## 四、后端部署

### 1. 安装依赖

```powershell
cd D:/pet/backend
npm install --registry=https://registry.npmmirror.com
```

### 2. 配置环境变量

后端使用 `dotenv`，启动时从 `backend/.env` 读取。创建 `backend/.env`（参考 `docker-compose.yml` 的 `environment`）：

```env
NODE_ENV=production
PORT=3000
JWT_SECRET=请改成你自己的随机密钥
JWT_EXPIRES_IN=7d
FRONTEND_URL=https://你的前端域名或http://localhost:3000
AI_API_KEY=你的火山方舟API_KEY
AI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
AI_MODEL=doubao-seed-2-0-pro-260215
```

环境变量说明（取自 `src/server.js`、`src/middleware/auth.js`、`src/routes/*`）：
- `PORT`：服务端口，默认 3000。
- `JWT_SECRET`：JWT 签名密钥，**生产务必修改**，否则有安全风险。
- `FRONTEND_URL`：CORS 放行来源（用于跨域与邀请链接生成）。同源部署可留空。
- `AI_API_KEY` / `AI_BASE_URL` / `AI_MODEL`：AI 教练功能所需，不用的话留空，相关接口会降级。

### 3. 初始化数据库（关键）

迁移前需确保 `backend/data/` 目录存在（`better-sqlite3` 不会自动创建目录）。`scripts/setup.js` **已内置自动创建 `data/` 目录**，直接执行：

```powershell
node scripts/setup.js
```

该命令会执行迁移并填充种子数据，结尾打印基础账号（密码均为 `111111`）：
- 管理员：`admin`
- 教师：`teacher1` ~ `teacher10`
- 学生：`student1` ~ `student50`

> 仅清库重来：`node scripts/setup.js --clean`
> 仅跑迁移：`npx knex migrate:latest --knexfile knexfile.js --env production`
> 仅跑种子：`npx knex seed:run --knexfile knexfile.js --env production`

### 4. 启动服务

```powershell
npm start        # = node src/server.js，监听 PORT(默认3000)
```

生产环境建议用进程守护（任选其一）：
- **pm2**：`npm i -g pm2 && pm2 start src/server.js --name pet`
- **nohup**：`nohup npm start > backend.log 2>&1 &`
- **systemd**（Linux）：写一个 service 单元，替代 Docker 的 `restart: unless-stopped`。

---

## 五、前端编译与部署

### 1. 编译

```powershell
cd D:/pet/frontend
npm install --registry=https://registry.npmmirror.com
npm run build        # = tsc && vite build，产物输出到 frontend/dist/
```

> `npm run build` 含 TypeScript 类型检查，类型错误会中断编译。若仅要产物可临时用 `npx vite build`。

### 2. 前端如何连接后端（重要）

前端通过环境变量 `VITE_API_URL` 决定后端地址，默认 `/api`（同源相对路径）。相关代码：
- `src/utils/api.ts`：`const API_BASE_URL = import.meta.env.VITE_API_URL || '/api'`
- `src/components/ClassDashboard.tsx`、`ChatRoom.tsx`：`VITE_API_URL?.replace('/api','') || 'http://localhost:3000'`（**socket.io 聊天室**，未配置时 fallback 到 `localhost:3000`，外网访问会连不上，必须配置）

#### 方案 A：前端由后端托管（无需配 VITE_API_URL）

```powershell
# 复制编译产物与图片到后端 public 目录
xcopy D:/pet/frontend/dist\* D:/pet/backend/public\ /E /Y
xcopy D:/pet/frontend/public\images D:/pet/backend/public\images /E /Y
```

后端 `express.static('public')` 会托管这些文件。访问 `http://后端地址:3000` 即同时得到前后端，`/api` 自动指向本机后端，**无需改前端**。每次重新 `npm run build` 后需重新复制。

#### 方案 B：前端由 Nginx 托管（需配 VITE_API_URL）

在 `frontend/.env` 写入后端公网地址：

```env
VITE_API_URL=https://你的域名/api
```

然后重新编译 `npm run build`。Nginx 的 root 指向 `frontend/dist`，并把 `/api`、`/socket.io` 反向代理到后端 3000（见下一节）。

---

## 六、外网部署示例（宝塔面板 + Nginx，方案 B）

适用：已有域名（如 `pet2.uwper.cn`）指向服务器，使用宝塔面板管理 Nginx（Windows / Linux 皆可）。

### 1. Nginx 站点配置要点

网站 root 设为 `frontend/dist`。需补充两处：

**(a) SPA 路由回退（解决地址栏直输 404）**

React Router 是单页应用，所有前端路由都由 `index.html` 处理。在 `server` 块内、`root` 之后加：

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

否则直接访问 `https://域名/workspace?menu=admin` 会 404。

**(b) 反向代理（API + Socket.io）**

代理规则写到宝塔的 `proxy/你的域名/*.conf`（如 `D:/BtSoft/nginx/conf/vhost/proxy/pet2.uwper.cn/backend.conf`）：

```nginx
# 后端 API
location /api/ {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# Socket.io 聊天室（必须支持 WebSocket 升级）
location /socket.io/ {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_read_timeout 600s;
}
```

> `location /api/` 比 `location /` 更具体，会优先匹配，不会被 SPA 回退拦截。

### 2. 前端 .env（必须，否则聊天室连 localhost）

```env
VITE_API_URL=https://pet2.uwper.cn/api
```

改完后重新 `npm run build`，产物自动进入宝塔 root 指向的 `dist/`。

### 3. 后端 .env 的 FRONTEND_URL

```env
FRONTEND_URL=https://pet2.uwper.cn
```

改完重启后端，否则浏览器可能被 CORS 拦截。

### 4. HTTPS / SSL

宝塔可一键申请 Let's Encrypt 证书；申请后把 `VITE_API_URL` 改为 `https://...` 再 build 一次。注意 `ssl_protocols` 建议至少包含 TLSv1.2。

### 5. 重载与验证

- 宝塔面板「Nginx → 重载配置」，或命令行 `nginx -s reload`。
- 浏览器硬刷（Ctrl+F5）访问 `https://域名/workspace?menu=admin`，应正常显示（不再 404）。
- F12 查看 Network：`/api/...` 返回 200；聊天室 socket.io 连接成功。

---

## 七、完整部署顺序速查

### 方案 A（后端托管前端，单机最快）
```powershell
# 后端
cd D:/pet/backend
npm install
# 创建 .env（FRONTEND_URL 可留 http://localhost:3000）
node scripts/setup.js
npm start

# 前端
cd D:/pet/frontend
npm install
# 无需 .env
npm run build
xcopy dist\* D:/pet/backend/public\ /E /Y
xcopy public\images D:/pet/backend/public\images /E /Y
```
访问 `http://localhost:3000`。

### 方案 B（Nginx 托管前端，外网正式）
```powershell
# 前端
cd D:/pet/frontend
echo VITE_API_URL=https://你的域名/api > .env
npm install
npm run build

# 后端
cd D:/pet/backend
npm install
# .env 配 FRONTEND_URL=https://你的域名 及 AI_*
node scripts/setup.js
pm2 start src/server.js --name pet

# 宝塔 Nginx：加 SPA 回退 + 反向代理 conf，重载
```
访问 `https://你的域名`。

---

## 八、常见问题

- **`Cannot open database because the directory does not exist`**：`data/` 目录不存在。`scripts/setup.js` 现已自动创建；老版本请手动 `mkdir data` 后重试。
- **地址栏直输前端路由 404**：缺 SPA 回退 `try_files ... /index.html`，见第六节 (a)。
- **聊天室连不上 / 一直重连**：前端未设 `VITE_API_URL`，socket fallback 到 `localhost:3000`；且 Nginx 未正确转发 `/socket.io/`（缺 `Upgrade`/`Connection` 头）。见第五节 2 与第六节 (b)。
- **CORS 报错**：后端 `FRONTEND_URL` 未包含前端来源；改为前端实际域名后重启后端。
- **接口全 500**：数据库未初始化，先跑 `node scripts/setup.js`。
- **better-sqlite3 安装/启动报错**：缺少原生编译环境，安装 python3 + make + g++ 后重装依赖。
```
